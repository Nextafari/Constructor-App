To create a the sign up page, the following must be done
  generate controller for the users
  generate a migration/database relationship to store the users from the form
  create a login authentication to retrieve users anytime they login
  to create two forms 
    for users to signup
    for employers to signup (to write a logic upon sign up for the user to select which category they fall into)
  to display two cards for the user to select after which the form will be rendered
  to create a column on the table for both users
  
  